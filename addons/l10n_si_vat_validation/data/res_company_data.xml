<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        ir.property was removed in Odoo 17+.
        The si_vat_check_enabled field on res.company has default=False
        in the Python definition, so no XML data record is needed.
        This file is intentionally empty to maintain backward compatibility
        with the manifest's 'data' list entry.
    -->
</odoo>
